export { PAGINATION } from '../config/pagination'; // ページネーション設定（ユーザー定義）

/**
 * ルートパス定数（URL設定）
 * 
 * 【解説】
 * ルートパス = ウェブサイトのURL構造を定義
 * 例：「/threads/123/posts」= スレッドID 123の投稿一覧ページ
 */
export { ROUTES } from '../config/routes'; // ルーティング定数（ユーザー定義）

/**
 * キャッシュ関連の設定値
 * 
 * 【解説】
 * キャッシュ = 一度取得したデータを一時保存して、再利用する仕組み
 * 【メリット】同じデータを何度もサーバーから取得する必要がなくなり、高速化される
 */
export { CACHE } from '../config/cache'; // キャッシュ設定（ユーザー定義）

/**
 * UI（ユーザーインターフェース）関連の定数
 * 
 * 【解説】
 * UI = ユーザーが操作する画面の見た目や動作に関する設定
 */
export { UI } from '../config/ui'; // UI定数（ユーザー定義）